application:
  xlsx-export-path: ./export/

server:
  mode: debug
  port: 8081

database:
  host: 127.0.0.1
  port: 5432
  user: postgres
  password: postgres
  name: sandbox
  schema: postgres
  generate-schema: false
  ssl-mode: disable