application:
  xlsx-export-path: ./export/

database:
  host: 127.0.0.1
  port: 5432
  user: postgres
  password: postgres
  name: sandbox
  schema: postgres
  generate-schema: true
  ssl-mode: disable