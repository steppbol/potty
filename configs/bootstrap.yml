application:
  xlsx-export-path: ./export/

server:
  mode: debug
  port: 8081

database:
  host: postgres
  port: 5432
  user: postgres
  password: postgres
  name: sandbox
  schema: postgres
  generate-schema: true
  ssl-mode: disable

cache:
  host: redis
  port: 6379
  password: